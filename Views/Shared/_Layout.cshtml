<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title | English Mastery Portal</title>

    <link href="@Url.Content("~/css/style.css")" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
</head>
<body>

    <div class="header" style="padding-bottom: 0;">
        <div class="container">
            <div class="navbar" style="display: flex; justify-content: space-between; align-items: center; padding: 15px 0;">
                <div class="logo">
                    <img src="~/Content/images/logo-one.jpg" width="50px" alt="Logo" />
                </div>
                <nav>
                    <ul id="MenuItems">
                        <li><a href="@Url.Action("Index", "Home")">Home</a></li>
                        <li><a href="@Url.Action("Course", "Course")">Courses</a></li>

                        @if (Session["UserAccount"] == null || Session["UserRole"] == null)
                        {
                            // 1. GIAO DIỆN KHI CHƯA ĐĂNG NHẬP
                            <li><a href="@Url.Action("Register", "User")">Register</a></li>
                            <li><a href="@Url.Action("Login", "User")">Login</a></li>
                        }
                        else
                        {
                            // Đọc thẳng vai trò từ Session dạng chuỗi chữ (Rất an toàn và nhẹ hệ thống)
                            string userRole = Session["UserRole"].ToString();
                            string displayName = Session["UserAccount"].ToString();

                            // 2. PHÂN HỆ: STUDENT
                            if (userRole == "Student")
                            {
                                <li><a href="@Url.Action("MyCourses", "Student")">My Courses</a></li>
                                <li><a href="@Url.Action("Payment", "Student")">Payment</a></li>
                                <li>
                                    <a href="@Url.Action("Profile", "Student")" class="student-welcome" style="font-weight: 600; color: #764ba2;">
                                        <i class="fa fa-user-circle"></i> Welcome, @displayName
                                    </a>
                                </li>
                            }

                            // 3. PHÂN HỆ: INSTRUCTOR (Khi lượn lờ ra trang ngoài)
                            else if (userRole == "Instructor")
                            {
                                // Cho phép họ bấm nút này để quay vào trong vùng quản lý của họ bất cứ lúc nào
                                <li>
                                    <a href="@Url.Action("Dashboard", "Instructor")" style="background: #4cd137; color: #fff !important; padding: 5px 12px; border-radius: 4px; font-weight: 600; text-decoration: none;">
                                        <i class="fa fa-arrow-left"></i> Return Instructor Mode (@displayName)
                                    </a>
                                </li>
                            }

                            // 4. PHÂN HỆ: ADMIN (Khi lượn lờ ra trang ngoài)
                            else if (userRole == "Admin")
                            {
                                // Cho phép Admin quay lại Admin Panel thần thánh
                                <li>
                                    <a href="@Url.Action("Dashboard", "Admin")" style="background: #eccc68; color: #000 !important; padding: 5px 12px; border-radius: 4px; font-weight: 600; text-decoration: none;">
                                        <i class="fa fa-user-shield"></i> Return Admin panel
                                    </a>
                                </li>
                            }

                            // NÚT LOGOUT CHUNG
                            <li><a href="@Url.Action("Logout", "User")" style="color: #ff523b; font-size: 13px; font-weight: 500;"><i class="fa fa-sign-out"></i> Logout</a></li>
                        }
                    </ul>
                </nav>
            </div>
        </div>
    </div>

    <div class="main-content">
        @RenderBody()
    </div>

    <div class="footer mt-50">
        <div class="container">
            <div class="row">
                <div class="footer-col-1"><h3>Resources</h3><p>Study anytime, anywhere.</p></div>
                <div class="footer-col-2"><img src="~/Content/images/logo-one.jpg" alt="Logo"><p>Professional English Training.</p></div>
                <div class="footer-col-3"><h3>Quick Links</h3><ul><li><a href="#">Certifications</a></li><li><a href="#">Support</a></li></ul></div>
            </div>
            <hr />
            <p class="copyright">Copyright 2026 - Team 03 - English Mastery Portal</p>
        </div>
    </div>

</body>
</html>